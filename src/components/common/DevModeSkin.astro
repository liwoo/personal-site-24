---
import { Icon } from 'astro-icon/components';
import { headerData } from '~/data/navigation';
/**
 * Dev Mode skin — the styles + fixed chrome for the whole-site "editorial JSON"
 * redesign. In Dev Mode (`html.dev-mode`) every page's normal render
 * (`[data-dev-normal]`) is hidden and its artistic JSON document
 * (`[data-dev-json]`, produced by DevDoc.astro) is shown full-bleed. Styling
 * classes are `.aj-*`; see `artisticJson.ts` for the markup they target.
 */
const navItems = [
  { label: 'home', href: '/' },
  ...headerData.links.map((l) => ({ label: String(l.text).toLowerCase(), href: l.href })),
];
---

<!-- Left tmux-style navigation index (fixed; hamburger toggles it on mobile) -->
<button
  type="button"
  class="dmj-hamburger"
  data-dmj-hamburger
  aria-label="Toggle navigation"
  aria-expanded="false"
>
  <span></span><span></span><span></span>
</button>
<aside class="dmj-nav" data-dmj-nav aria-label="Dev Mode navigation">
  <div class="dmj-nav-brand"><span class="dmj-nav-prompt">~/</span>chienda<span class="dmj-nav-blink">▊</span></div>
  <nav class="dmj-nav-list">
    {
      navItems.map((item, i) => (
        <a class="dmj-nav-item" href={item.href} data-route={item.href}>
          <span class="dmj-nav-idx">{String(i).padStart(2, '0')}</span>
          <span class="dmj-nav-name">~/{item.label}</span>
        </a>
      ))
    }
  </nav>
  <div class="dmj-nav-context" data-dmj-context hidden>
    <div class="dmj-nav-ctx-title">// on this page</div>
    <nav class="dmj-nav-sections" data-dmj-sections></nav>
  </div>
  <div class="dmj-nav-foot">
    <div>dev_mode // tmux</div>
    <div class="dmj-nav-path">~ / <b data-dmj-path>index</b>.json</div>
  </div>
</aside>

<!-- Fixed exit control (the header toggle is hidden in Dev Mode) -->
<button type="button" class="dmj-exit" aria-label="Exit Dev Mode" data-aw-toggle-dev-mode>
  <Icon name="tabler:x" class="w-3.5 h-3.5" /> exit dev
</button>
<div class="dmj-sidebar" aria-hidden="true">
  <div>build_ver: 2.0.26</div>
  <div>render_engine: editorial_json</div>
  <div>status: dev_mode</div>
</div>

<style is:global>
  /* --- Visibility: swap the whole page in/out of Dev Mode --------------- */
  [data-dev-json] {
    display: none;
  }
  html.dev-mode [data-dev-normal] {
    display: none;
  }
  html.dev-mode [data-dev-json] {
    display: block;
  }
  html.dev-mode body {
    background-color: #0a0a0c;
  }

  /* --- Document shell --------------------------------------------------- */
  .aj-doc {
    position: relative;
    overflow: hidden;
    min-height: 100vh;
    width: 100%;
    padding: 2rem;
    background: #0a0a0c;
    color: #d1d1d1;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, 'Space Mono', monospace;
  }
  @media (min-width: 768px) {
    .aj-doc {
      padding: 5rem;
    }
  }
  @media (min-width: 1024px) {
    html.dev-mode .aj-doc {
      padding-left: 17rem;
    }
  }
  .aj-doc ::selection {
    background: rgba(6, 182, 212, 0.3);
    color: #fff;
  }
  .aj-grid {
    position: fixed;
    inset: 0;
    pointer-events: none;
    opacity: 0.18;
    background-image: radial-gradient(#2a2a2a 1px, transparent 1px);
    background-size: 30px 30px;
  }
  .aj-shell {
    position: relative;
    z-index: 10;
    max-width: 64rem;
    margin: 0 auto;
  }

  /* --- Reveal stagger --------------------------------------------------- */
  .aj-reveal {
    opacity: 0;
    transform: translateY(20px);
    animation: ajRise 0.55s cubic-bezier(0.22, 1, 0.36, 1) forwards;
    animation-delay: calc(var(--i, 0) * 0.1s + 0.2s);
  }
  .aj-front.aj-reveal {
    animation-delay: 0.08s;
  }
  @keyframes ajRise {
    to {
      opacity: 1;
      transform: none;
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .aj-reveal {
      animation: none;
      opacity: 1;
      transform: none;
    }
  }

  /* --- Front matter ----------------------------------------------------- */
  .aj-front {
    position: relative;
    margin-bottom: 6rem;
    padding-left: 1rem;
  }
  .aj-front-rule {
    position: absolute;
    left: -0.25rem;
    top: 0;
    bottom: 0;
    width: 4px;
    opacity: 0.5;
    background: linear-gradient(to bottom, #f59e0b, transparent);
  }
  .aj-dashes {
    color: rgba(245, 158, 11, 0.8);
    font-weight: 700;
    letter-spacing: 0.1em;
    font-size: 0.75rem;
  }
  .aj-meta {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.5rem 2rem;
    font-size: 0.875rem;
    line-height: 1.65;
    margin: 0.5rem 0;
  }
  @media (min-width: 768px) {
    .aj-meta {
      grid-template-columns: 150px 1fr;
    }
  }
  .aj-fkey {
    color: #6b7280;
    font-style: italic;
  }
  .aj-fval {
    color: #fff;
  }
  .aj-fval.strong {
    font-weight: 600;
  }
  .aj-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    align-items: center;
  }
  .aj-chip {
    border: 1px solid rgba(34, 211, 238, 0.25);
    color: #22d3ee;
    padding: 0.125rem 0.5rem;
    border-radius: 0.25rem;
    font-size: 10px;
    line-height: 1.6;
  }
  .aj-chip--link {
    text-decoration: none;
    transition: background 0.15s ease, color 0.15s ease;
  }
  .aj-chip--link:hover {
    background: #22d3ee;
    color: #0a0a0c;
  }

  /* --- Root + sections -------------------------------------------------- */
  .aj-body {
    display: flex;
    flex-direction: column;
    gap: 8rem;
  }
  .aj-root-open {
    color: #06b6d4;
    font-size: 1.5rem;
    font-weight: 300;
    opacity: 0.5;
    margin-bottom: 3rem;
  }
  .aj-root-close {
    color: #06b6d4;
    font-size: 2.25rem;
    font-weight: 300;
    opacity: 0.3;
    margin-top: 8rem;
  }
  .aj-section {
    position: relative;
  }
  .aj-watermark {
    position: absolute;
    top: -3rem;
    left: -1.5rem;
    pointer-events: none;
    z-index: 0;
    font-size: clamp(2.5rem, 9vw, 4.5rem);
    font-weight: 900;
    line-height: 1;
    letter-spacing: -0.02em;
    color: #16161a;
    transition: color 0.3s ease;
    white-space: nowrap;
  }
  .aj-section:hover .aj-watermark {
    color: rgba(6, 182, 212, 0.05);
  }
  .aj-brace {
    position: relative;
    z-index: 1;
    color: #06b6d4;
    font-size: 1.5rem;
    font-weight: 300;
  }
  .aj-indent {
    position: relative;
    z-index: 1;
    margin-left: 2rem;
  }
  @media (min-width: 768px) {
    .aj-indent {
      margin-left: 4rem;
    }
  }
  .aj-key-display {
    display: block;
    font-size: clamp(1.6rem, 8vw, 4.5rem);
    font-weight: 900;
    letter-spacing: -0.05em;
    color: #fff;
    opacity: 0.92;
    text-transform: uppercase;
    margin: 1rem 0;
    line-height: 1;
  }
  .aj-colon {
    color: #374151;
  }
  .aj-fields {
    margin-left: 1rem;
    border-left: 1px solid rgba(255, 255, 255, 0.06);
    padding-left: 2rem;
  }
  .aj-field {
    font-size: clamp(1.05rem, 2.4vw, 1.5rem);
    color: #9ca3af;
    line-height: 1.6;
    max-width: 38rem;
    margin: 0 0 1rem;
    overflow-wrap: anywhere;
    word-break: break-word;
  }
  /* Long string values wrap within their quotes to save width (esp. mobile). */
  .aj-str,
  .aj-link,
  .aj-val,
  .aj-item-title,
  .aj-key-display,
  .aj-exp-key {
    overflow-wrap: anywhere;
    word-break: break-word;
  }
  .aj-field .aj-k {
    color: #fcd34d;
  }
  .aj-field--chips {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.4rem;
  }
  .aj-subfields {
    margin-left: 1.5rem;
    border-left: 1px solid rgba(255, 255, 255, 0.05);
    padding-left: 1.25rem;
  }
  .aj-str {
    color: #fff;
  }
  .aj-num {
    color: #22d3ee;
  }
  .aj-bool {
    color: #f59e0b;
  }
  .aj-null {
    color: #f87171;
  }
  .aj-punc {
    color: #06b6d4;
  }
  .aj-link {
    color: #22d3ee;
    text-decoration: none;
  }
  .aj-link:hover {
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  /* --- Array sections + items ------------------------------------------ */
  .aj-exp-head {
    position: relative;
    z-index: 1;
    color: #06b6d4;
    font-size: 1.5rem;
    font-weight: 300;
    margin-bottom: 2rem;
  }
  .aj-exp-key {
    color: rgba(255, 255, 255, 0.85);
    text-transform: uppercase;
    font-weight: 900;
    font-size: clamp(1.75rem, 6vw, 2.25rem);
    letter-spacing: -0.05em;
    margin-right: 0.5rem;
  }
  .aj-bracket {
    color: #06b6d4;
  }
  .aj-array {
    display: flex;
    flex-direction: column;
    gap: 5rem;
    padding-top: 1rem;
    border-left: 1px solid rgba(6, 182, 212, 0.12);
  }
  .aj-exp-close {
    position: relative;
    z-index: 1;
    color: #06b6d4;
    font-size: 1.5rem;
    font-weight: 300;
    margin-top: 2rem;
  }
  .aj-item {
    position: relative;
    padding-left: 3rem;
  }
  .aj-item-tick {
    position: absolute;
    left: 0;
    top: 1.5rem;
    width: 2rem;
    height: 1px;
    background: rgba(6, 182, 212, 0.3);
  }
  .aj-brace-sm {
    color: #6b7280;
    font-size: 1.125rem;
  }
  .aj-item-body {
    margin: 0.1rem 0;
  }
  .aj-item-title {
    font-size: 1.5rem;
    font-weight: 700;
    color: #fff;
    margin: 0.15rem 0 0.4rem;
    transition: color 0.2s ease;
  }
  .aj-item:hover .aj-item-title {
    color: #22d3ee;
  }
  .aj-line {
    color: #6b7280;
    font-size: 0.9rem;
    line-height: 1.75;
    max-width: 40rem;
    overflow-wrap: anywhere;
    word-break: break-word;
  }
  .aj-line .aj-lk {
    color: #9ca3af;
    font-style: italic;
  }
  .aj-item--scalar {
    padding-left: 3rem;
    color: #fff;
    font-size: 1.05rem;
  }

  /* --- Creative image rendering (inside the JSON doc) ------------------- */
  .aj-figure {
    display: inline-block;
    position: relative;
    line-height: 0;
    width: fit-content;
    max-width: min(100%, 24rem);
    margin: 0.4rem 0;
    border: 1px solid rgba(34, 211, 238, 0.35);
    border-radius: 0.5rem;
    overflow: hidden;
    background: #0d0d10;
    vertical-align: middle;
  }
  .aj-figure img {
    display: block;
    width: 100%;
    height: auto;
    filter: grayscale(0.5) contrast(1.05);
    transition: filter 0.35s ease;
  }
  .aj-figure:hover img {
    filter: none;
  }
  .aj-figure::after {
    content: 'img';
    position: absolute;
    top: 0;
    right: 0;
    z-index: 2;
    padding: 1px 6px;
    font: 9px/1.4 ui-monospace, SFMono-Regular, monospace;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #0a0a0c;
    background: #f59e0b;
    border-bottom-left-radius: 0.4rem;
  }
  .aj-img-cap {
    position: absolute;
    left: 0;
    bottom: 0;
    z-index: 2;
    max-width: 100%;
    box-sizing: border-box;
    padding: 2px 6px;
    font: 10px/1.4 ui-monospace, SFMono-Regular, monospace;
    color: #22d3ee;
    background: rgba(0, 0, 0, 0.7);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    border-top-right-radius: 0.4rem;
  }

  /* --- Blink (shared by carets + nav cursor) --------------------------- */
  @keyframes ajBlink {
    50% {
      opacity: 0;
    }
  }

  /* --- Left tmux navigation sidebar ------------------------------------ */
  .dmj-nav,
  .dmj-hamburger {
    display: none;
  }
  html.dev-mode .dmj-nav {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    position: fixed;
    top: 0;
    left: 0;
    bottom: 0;
    width: 14rem;
    z-index: 9997;
    padding: 1.5rem 1rem;
    background: #08080a;
    border-right: 1px solid rgba(34, 211, 238, 0.15);
    font: 12px/1.5 ui-monospace, SFMono-Regular, monospace;
    transform: translateX(0);
    transition: transform 0.25s ease;
    overflow-y: auto;
  }
  html.dev-mode .dmj-nav-brand {
    color: #e5e7eb;
    font-weight: 700;
    letter-spacing: 0.05em;
  }
  .dmj-nav-prompt {
    color: #f59e0b;
  }
  .dmj-nav-blink {
    color: #22d3ee;
    animation: ajBlink 1s step-end infinite;
  }
  .dmj-nav-list {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }
  .dmj-nav-item {
    display: flex;
    gap: 0.6rem;
    align-items: baseline;
    padding: 0.35rem 0.5rem;
    border-radius: 0.3rem;
    color: #6b7280;
    text-decoration: none;
    transition: color 0.15s ease, background 0.15s ease;
  }
  .dmj-nav-item:hover {
    color: #e5e7eb;
    background: rgba(255, 255, 255, 0.03);
  }
  .dmj-nav-idx {
    color: #374151;
    font-size: 10px;
  }
  .dmj-nav-item.active {
    color: #22d3ee;
    background: rgba(6, 182, 212, 0.08);
  }
  .dmj-nav-item.active .dmj-nav-idx {
    color: #22d3ee;
  }
  /* Contextual (in-page) section index — a tmux-like pane list */
  .dmj-nav-context {
    border-top: 1px dashed rgba(255, 255, 255, 0.08);
    padding-top: 1rem;
  }
  .dmj-nav-ctx-title {
    color: #4b5563;
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.5rem;
  }
  .dmj-nav-sections {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
  }
  .dmj-nav-sec {
    display: flex;
    gap: 0.5rem;
    align-items: baseline;
    padding: 0.2rem 0.5rem;
    border-left: 2px solid transparent;
    color: #6b7280;
    text-decoration: none;
    font-size: 11px;
  }
  .dmj-nav-sec:hover {
    color: #e5e7eb;
  }
  .dmj-nav-sec-idx {
    color: #374151;
    font-size: 9px;
  }
  .dmj-nav-sec.active {
    color: #f59e0b;
    border-left-color: #f59e0b;
    background: rgba(245, 158, 11, 0.06);
  }
  .dmj-nav-sec.active .dmj-nav-sec-idx {
    color: #f59e0b;
  }
  .dmj-nav-foot {
    margin-top: auto;
    color: #4b5563;
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }
  .dmj-nav-path {
    text-transform: lowercase;
    margin-top: 0.4rem;
  }
  .dmj-nav-path b {
    color: #22d3ee;
    font-weight: 400;
  }
  @media (max-width: 1023px) {
    html.dev-mode .dmj-nav {
      transform: translateX(-100%);
      box-shadow: 0 0 40px rgba(0, 0, 0, 0.6);
      /* clear the fixed hamburger so the ~/chienda brand isn't hidden */
      padding-top: 4.5rem;
    }
    html.dev-mode .dmj-nav.open {
      transform: translateX(0);
    }
    html.dev-mode .dmj-hamburger {
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 4px;
      position: fixed;
      top: 1rem;
      left: 1rem;
      z-index: 9999;
      width: 2.4rem;
      height: 2.4rem;
      padding: 0.55rem;
      background: #0a0a0c;
      border: 1px solid rgba(34, 211, 238, 0.3);
      border-radius: 0.4rem;
      cursor: pointer;
    }
    html.dev-mode .dmj-hamburger span {
      display: block;
      height: 2px;
      background: #22d3ee;
    }
  }

  /* --- Terminal inputs ------------------------------------------------- */
  .aj-input {
    display: block;
    margin: 0 0 1.1rem;
  }
  .aj-input-label {
    display: block;
    color: #9ca3af;
    font-size: 0.8rem;
    font-style: italic;
    margin-bottom: 0.35rem;
  }
  .aj-input-line {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    max-width: 34rem;
    padding: 0.6rem 0.8rem;
    border: 1px solid rgba(34, 211, 238, 0.25);
    border-radius: 0.4rem;
    background: rgba(255, 255, 255, 0.02);
  }
  .aj-input-line:focus-within {
    border-color: #22d3ee;
    box-shadow: 0 0 0 2px rgba(34, 211, 238, 0.12);
  }
  .aj-prompt {
    color: #f59e0b;
    font-weight: 700;
  }
  .aj-input input,
  .aj-input textarea {
    flex: 1;
    min-width: 0;
    background: transparent;
    border: none;
    outline: none;
    color: #fff;
    font-family: inherit;
    font-size: 0.95rem;
    resize: vertical;
  }
  .aj-input input::placeholder,
  .aj-input textarea::placeholder {
    color: #4b5563;
  }
  .aj-caret {
    flex: none;
    width: 0.5rem;
    height: 1.1rem;
    background: #22d3ee;
    animation: ajBlink 1s step-end infinite;
  }
  .aj-input-line:focus-within .aj-caret {
    display: none;
  }

  /* --- Terminal buttons [[ ... ]] -------------------------------------- */
  .aj-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    margin-top: 0.6rem;
    padding: 0.6rem 1rem;
    border: 1px solid rgba(34, 211, 238, 0.4);
    border-radius: 0.4rem;
    background: rgba(6, 182, 212, 0.06);
    color: #22d3ee;
    font-family: inherit;
    font-size: 0.8rem;
    text-transform: uppercase;
    text-decoration: none;
    cursor: pointer;
    transition: background 0.2s ease, color 0.2s ease;
  }
  .aj-btn:hover {
    background: #22d3ee;
    color: #0a0a0c;
  }
  .aj-btn-br {
    color: #f59e0b;
    font-weight: 700;
  }
  .aj-btn:hover .aj-btn-br {
    color: #0a0a0c;
  }
  .aj-btn-label {
    letter-spacing: 0.25em;
  }

  /* --- Beautified terminal cards (footer, forms, CTAs) ----------------- */
  .aj-card {
    position: relative;
    overflow: hidden;
    padding: 2rem;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 1rem;
    background: rgba(255, 255, 255, 0.02);
  }
  @media (min-width: 768px) {
    .aj-card {
      padding: 2.5rem 3rem;
    }
  }
  .aj-card-wm {
    position: absolute;
    right: -0.5rem;
    bottom: -2.5rem;
    pointer-events: none;
    font-size: clamp(4rem, 14vw, 9rem);
    font-weight: 900;
    letter-spacing: -0.03em;
    color: rgba(255, 255, 255, 0.02);
  }
  .aj-eyebrow {
    display: inline-block;
    margin-bottom: 1.25rem;
    color: #f59e0b;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    font-size: 0.72rem;
    text-decoration: underline;
    text-decoration-color: rgba(245, 158, 11, 0.3);
    text-underline-offset: 8px;
  }
  .aj-card-body {
    position: relative;
    z-index: 1;
  }

  /* --- Creative logo wall ---------------------------------------------- */
  .aj-logos {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }
  .aj-logo {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    gap: 0.4rem;
  }
  .aj-logo img {
    width: 3.6rem;
    height: 3.6rem;
    object-fit: contain;
    padding: 0.45rem;
    border: 1px solid rgba(34, 211, 238, 0.2);
    border-radius: 0.5rem;
    /* light tile so brand symbols (often dark-on-white) stay legible */
    background: #f4f4f5;
    filter: grayscale(1) contrast(1.05);
    transition: filter 0.3s ease;
  }
  .aj-logo:hover img {
    filter: none;
  }
  .aj-logo-mark {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 3.5rem;
    height: 3.5rem;
    border: 1px solid rgba(34, 211, 238, 0.25);
    border-radius: 0.5rem;
    background: #0d0d10;
    color: #22d3ee;
    font-weight: 700;
    font-size: 1rem;
  }
  .aj-logo-cap {
    color: #6b7280;
    font-size: 10px;
  }

  /* --- Typewriter ------------------------------------------------------ */
  .aj-type {
    border-right: 0.1em solid #22d3ee;
    animation: ajBlink 1s step-end infinite;
  }
  .aj-type.aj-type--done {
    border-right-color: transparent;
    animation: none;
  }

  /* --- Prose (bios, article body) -------------------------------------- */
  .aj-prose {
    max-width: 44rem;
  }
  .aj-p {
    position: relative;
    margin: 0 0 1.1rem;
    padding-left: 1.5rem;
    color: #cbd5e1;
    font-size: 1rem;
    line-height: 1.75;
  }
  .aj-p-mark {
    position: absolute;
    left: 0;
    color: rgba(34, 211, 238, 0.5);
  }

  /* --- Rich article (themed markdown) ---------------------------------- */
  .aj-article {
    max-width: 46rem;
    color: #cbd5e1;
    font-size: 1rem;
    line-height: 1.8;
  }
  .aj-article .aj-p {
    padding-left: 0;
    margin: 0 0 1.25rem;
    color: #cbd5e1;
  }
  .aj-article .aj-h {
    margin: 2.25rem 0 1rem;
    color: #fff;
    font-weight: 800;
    line-height: 1.25;
    letter-spacing: -0.01em;
  }
  .aj-article .aj-h1 {
    font-size: 1.8rem;
  }
  .aj-article .aj-h2 {
    font-size: 1.45rem;
  }
  .aj-article .aj-h3 {
    font-size: 1.2rem;
  }
  .aj-article .aj-h4 {
    font-size: 1.05rem;
  }
  .aj-h-mark {
    color: #f59e0b;
    margin-right: 0.5rem;
    font-weight: 700;
  }
  .aj-article .aj-b {
    color: #fff;
    font-weight: 700;
  }
  .aj-article em {
    color: #e5e7eb;
    font-style: italic;
  }
  .aj-ic {
    padding: 0.05rem 0.35rem;
    border-radius: 0.25rem;
    background: rgba(34, 211, 238, 0.1);
    border: 1px solid rgba(34, 211, 238, 0.2);
    color: #7dd3fc;
    font-size: 0.85em;
  }
  .aj-article .aj-link {
    text-decoration: underline;
    text-decoration-color: rgba(34, 211, 238, 0.4);
    text-underline-offset: 3px;
  }
  .aj-ul {
    margin: 0 0 1.25rem;
    padding-left: 1.5rem;
    list-style: none;
  }
  .aj-ul li {
    position: relative;
    margin: 0.4rem 0;
    color: #cbd5e1;
  }
  .aj-ul li::before {
    content: '›';
    position: absolute;
    left: -1.1rem;
    color: #22d3ee;
  }
  .aj-quote {
    margin: 1.5rem 0;
    padding: 0.5rem 0 0.5rem 1.25rem;
    border-left: 3px solid #f59e0b;
    color: #e5e7eb;
    font-style: italic;
  }
  .aj-pre {
    margin: 1.25rem 0;
    padding: 1rem 1.25rem;
    overflow-x: auto;
    background: #0d0d10;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 0.6rem;
  }
  .aj-pre code {
    color: #c9d1d9;
    font-size: 0.85rem;
    line-height: 1.6;
    white-space: pre;
  }
  /* Syntax highlighting tokens */
  .aj-pre .tk-com {
    color: #8b949e;
    font-style: italic;
  }
  .aj-pre .tk-str {
    color: #a5d6ff;
  }
  .aj-pre .tk-num {
    color: #79c0ff;
  }
  .aj-pre .tk-lit {
    color: #79c0ff;
  }
  .aj-pre .tk-kw {
    color: #ff7b72;
  }
  .aj-pre .tk-punc {
    color: #c9d1d9;
  }
  .aj-figure-block {
    margin: 1.5rem 0;
  }
  .aj-figcap {
    margin-top: 0.5rem;
    color: #6b7280;
    font-size: 0.8rem;
    font-style: italic;
  }
  .aj-figcap--solo {
    padding-left: 0.85rem;
    border-left: 2px solid rgba(34, 211, 238, 0.3);
  }

  /* --- Share row ------------------------------------------------------- */
  .aj-share {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
  }
  .aj-share-btn {
    margin-top: 0;
  }

  /* --- Dot-matrix block banner (home header) --------------------------- */
  .aj-banner {
    display: inline-flex;
    flex-direction: column;
    gap: clamp(1.5px, 0.3vw, 4px);
    max-width: 100%;
    margin: 0 0 2.75rem;
    overflow-x: auto;
  }
  .aj-brow {
    display: flex;
    gap: clamp(1.5px, 0.3vw, 4px);
  }
  .aj-bpx {
    flex: none;
    width: clamp(5px, 1.1vw, 15px);
    height: clamp(4px, 0.72vw, 9px);
    border-radius: 2px;
    background: transparent;
  }
  .aj-bpx.on {
    background: #ededed;
    box-shadow: 0 0 7px rgba(34, 211, 238, 0.18);
  }

  /* --- Tweet card (articles + featured bio tweet) ---------------------- */
  .aj-tweet {
    max-width: 40rem;
    margin: 1.75rem 0;
    padding: 1.25rem 1.5rem;
    border: 1px solid rgba(34, 211, 238, 0.25);
    border-radius: 0.9rem;
    background: rgba(34, 211, 238, 0.03);
  }
  .aj-tweet-head {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
  }
  .aj-tweet-x {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.5rem;
    height: 1.5rem;
    border-radius: 0.35rem;
    background: #e5e7eb;
    color: #0a0a0c;
    font-weight: 700;
  }
  .aj-tweet-author {
    color: #fff;
    font-weight: 700;
  }
  .aj-tweet-handle {
    color: #6b7280;
    font-size: 0.85rem;
  }
  .aj-tweet-text {
    color: #d1d5db;
    font-size: 1.02rem;
    line-height: 1.7;
  }
  /* --- Testimony card -------------------------------------------------- */
  .aj-testimony {
    display: flex;
    gap: 1.25rem;
    align-items: flex-start;
    max-width: 44rem;
  }
  .aj-testimony-avatar {
    flex: none;
  }
  .aj-testimony-avatar .aj-figure {
    width: 4rem;
    height: 4rem;
    margin: 0;
  }
  .aj-testimony-avatar .aj-figure img {
    width: 4rem;
    height: 4rem;
    object-fit: cover;
  }
  .aj-testimony-quote {
    color: #e5e7eb;
    font-size: 1.05rem;
    line-height: 1.75;
    font-style: italic;
  }
  .aj-testimony-by {
    margin-top: 0.9rem;
    color: #9ca3af;
    font-size: 0.9rem;
  }

  .aj-tweet-link {
    display: inline-block;
    margin-top: 0.9rem;
    color: #22d3ee;
    font-size: 0.8rem;
    text-decoration: none;
  }
  .aj-tweet-link:hover {
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  /* --- Shareable code block -------------------------------------------- */
  .aj-codeblock {
    margin: 1.5rem 0;
    max-width: 100%;
  }
  .aj-codebar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.4rem 0.9rem;
    background: #161b22;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-bottom: none;
    border-radius: 0.6rem 0.6rem 0 0;
  }
  .aj-code-lang {
    color: #f59e0b;
    font: 11px/1 ui-monospace, SFMono-Regular, monospace;
    letter-spacing: 0.05em;
  }
  .aj-code-actions {
    display: inline-flex;
    gap: 0.4rem;
  }
  .aj-code-btn {
    padding: 0.2rem 0.55rem;
    font: 10px/1 ui-monospace, SFMono-Regular, monospace;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: #6b7280;
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 0.3rem;
    cursor: pointer;
    transition: color 0.2s ease, border-color 0.2s ease;
  }
  .aj-code-btn:hover {
    color: #22d3ee;
    border-color: rgba(34, 211, 238, 0.4);
  }
  .aj-codeblock .aj-copy-body {
    margin: 0;
  }
  .aj-codeblock .aj-pre {
    margin: 0;
    border-radius: 0 0 0.6rem 0.6rem;
  }

  /* --- Footer avatar (SEO image) --------------------------------------- */
  .aj-footer-top {
    position: relative;
    z-index: 1;
    display: flex;
    gap: 1.75rem;
    align-items: flex-start;
    margin-bottom: 1.75rem;
  }
  .aj-footer-avatar {
    flex: none;
  }
  .aj-footer-avatar .aj-figure {
    width: 6rem;
    height: 6rem;
    margin: 0;
  }
  .aj-footer-avatar .aj-figure img {
    width: 6rem;
    height: 6rem;
    object-fit: cover;
  }
  .aj-footer-top .aj-fgrid {
    flex: 1;
    margin-bottom: 0;
  }
  @media (max-width: 640px) {
    .aj-footer-top {
      flex-direction: column;
    }
  }

  /* --- Inset company logo + hover-reveal CV detail --------------------- */
  .aj-item-head {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 0.3rem;
  }
  .aj-item-head .aj-item-title {
    margin: 0;
  }
  .aj-item-logo {
    flex: none;
  }
  .aj-item-logo img {
    width: 2.4rem;
    height: 2.4rem;
    object-fit: contain;
    padding: 0.3rem;
    border: 1px solid rgba(34, 211, 238, 0.2);
    border-radius: 0.4rem;
    background: #f4f4f5;
    filter: grayscale(1);
    transition: filter 0.3s ease;
  }
  .aj-item:hover .aj-item-logo img {
    filter: none;
  }
  .aj-more-hint {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    margin-top: 0.6rem;
    color: #4b5563;
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    cursor: pointer;
    user-select: none;
  }
  .aj-more-hint:hover {
    color: #22d3ee;
  }
  .aj-more-i {
    color: #f59e0b;
    font-weight: 700;
  }
  .aj-item-more {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.35s ease, margin-top 0.35s ease;
  }
  .aj-more-line {
    position: relative;
    padding: 0.15rem 0 0.15rem 1rem;
    color: #9ca3af;
    font-size: 0.85rem;
    line-height: 1.7;
  }
  .aj-more-line::before {
    content: '›';
    position: absolute;
    left: 0;
    color: #22d3ee;
  }
  .aj-item.open .aj-item-more {
    max-height: 30rem;
    margin-top: 0.4rem;
  }
  @media (hover: hover) {
    .aj-item--expandable:hover .aj-item-more {
      max-height: 30rem;
      margin-top: 0.4rem;
    }
    .aj-item--expandable:hover .aj-more-hint {
      color: #22d3ee;
    }
  }

  /* --- Click-to-copy affordance ---------------------------------------- */
  .aj-copyable {
    position: relative;
  }
  .aj-copy {
    position: absolute;
    top: -0.4rem;
    right: 0;
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    padding: 0.25rem 0.55rem;
    font: 11px/1 ui-monospace, SFMono-Regular, monospace;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #6b7280;
    background: rgba(10, 10, 12, 0.85);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 0.35rem;
    cursor: pointer;
    opacity: 0;
    transition: opacity 0.2s ease, color 0.2s ease, border-color 0.2s ease;
  }
  .aj-copyable:hover .aj-copy,
  .aj-copy:focus-visible {
    opacity: 1;
  }
  .aj-copy:hover {
    color: #22d3ee;
    border-color: rgba(34, 211, 238, 0.3);
  }
  .aj-copy.copied {
    opacity: 1;
    color: #4ade80;
  }
  .aj-copy-i {
    color: #f59e0b;
  }

  /* --- External-link iconography --------------------------------------- */
  .aj-link[target='_blank']::after {
    content: '↗';
    margin-left: 0.15em;
    font-size: 0.78em;
    color: #f59e0b;
    vertical-align: 0.08em;
  }

  /* --- Mobile readability: full-width prose, less indentation ---------- */
  @media (max-width: 640px) {
    .aj-fields {
      margin-left: 0.35rem;
      padding-left: 0.9rem;
    }
    .aj-subfields {
      margin-left: 0.35rem;
      padding-left: 0.85rem;
    }
    .aj-array {
      padding-top: 0.5rem;
    }
    .aj-item-tick {
      width: 1rem;
    }
    .aj-field,
    .aj-line,
    .aj-prose,
    .aj-article {
      max-width: 100%;
    }
    .aj-article {
      font-size: 1.02rem;
      line-height: 1.8;
    }
    .aj-copy {
      position: static;
      opacity: 1;
      margin-bottom: 0.6rem;
    }
  }

  /* --- Form status line ------------------------------------------------ */
  .aj-form-status {
    margin-top: 0.75rem;
    min-height: 1.2rem;
    font-size: 0.85rem;
    color: #6b7280;
  }
  .aj-form-status.ok {
    color: #4ade80;
  }
  .aj-form-status.err {
    color: #f87171;
  }

  /* --- Footer card ----------------------------------------------------- */
  .aj-footer {
    margin-top: 7rem;
  }
  .aj-fgrid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
    gap: 1.5rem;
    margin-bottom: 1.75rem;
  }
  .aj-fcol-title {
    color: #fcd34d;
    font-size: 0.8rem;
    margin-bottom: 0.6rem;
  }
  .aj-flist {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
  }
  .aj-flist a {
    font-size: 0.85rem;
  }
  .aj-frow {
    position: relative;
    z-index: 1;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.4rem;
    margin: 0.5rem 0;
    color: #6b7280;
    font-size: 0.85rem;
  }
  .aj-fnote {
    position: relative;
    z-index: 1;
    margin-top: 1.25rem;
    color: #4b5563;
    font-size: 0.8rem;
  }

  /* --- Fixed chrome (exit + status) ------------------------------------ */
  .dmj-exit,
  .dmj-sidebar {
    display: none;
  }
  html.dev-mode .dmj-exit {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    position: fixed;
    top: 1rem;
    right: 1rem;
    z-index: 10000;
    padding: 0.4rem 0.7rem;
    font: 11px/1 ui-monospace, SFMono-Regular, monospace;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #0a0a0c;
    background: #22d3ee;
    border: none;
    border-radius: 0.4rem;
    cursor: pointer;
  }
  html.dev-mode .dmj-exit:hover {
    background: #67e8f9;
  }
  html.dev-mode .dmj-sidebar {
    display: block;
    position: fixed;
    right: 1.25rem;
    bottom: 1.1rem;
    z-index: 9998;
    pointer-events: none;
    font: 10px/1.6 ui-monospace, SFMono-Regular, monospace;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    text-align: right;
    color: #4b5563;
  }

  /* --- Image lightbox (tap to expand) ---------------------------------- */
  .aj-figure img,
  .aj-figure-block img {
    cursor: zoom-in;
  }
  .aj-lightbox {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 10001;
    align-items: center;
    justify-content: center;
    padding: 1rem;
    background: rgba(0, 0, 0, 0.93);
    cursor: zoom-out;
  }
  .aj-lightbox.open {
    display: flex;
  }
  .aj-lightbox img {
    max-width: 100%;
    max-height: 92vh;
    object-fit: contain;
    border: 1px solid rgba(34, 211, 238, 0.3);
    border-radius: 0.5rem;
  }
  .aj-lightbox-close {
    position: fixed;
    top: 1rem;
    right: 1rem;
    width: 2.4rem;
    height: 2.4rem;
    border-radius: 0.4rem;
    background: #0a0a0c;
    color: #22d3ee;
    border: 1px solid rgba(34, 211, 238, 0.3);
    font-size: 1rem;
    cursor: pointer;
  }
  .aj-search-count {
    margin-top: 0.5rem;
    color: #6b7280;
    font-size: 0.8rem;
  }

  /* --- Small screens: tighten padding, 2-col footer, shrink type -------- */
  @media (max-width: 640px) {
    .aj-key-display {
      font-size: clamp(1.5rem, 9vw, 2.4rem);
    }
    .aj-exp-key {
      font-size: clamp(1.3rem, 8vw, 1.9rem);
    }
    .aj-indent {
      margin-left: 0.75rem;
    }
    .aj-item {
      padding-left: 1.5rem;
    }
    .aj-doc {
      /* extra top padding so content clears the fixed hamburger + exit button */
      padding: 4.25rem 0.75rem 1.5rem;
    }
    .aj-banner {
      margin-bottom: 2rem;
    }
    /* reclaim horizontal space eaten by card padding on phones */
    .aj-card {
      padding: 1.1rem 0.9rem;
    }
    .aj-fields {
      margin-left: 0.2rem;
      padding-left: 0.7rem;
    }
    .aj-prose,
    .aj-article {
      max-width: 100%;
    }
    /* footer link columns: two columns on mobile */
    .aj-fgrid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem 0.75rem;
    }
    /* keep the status badge from colliding with content */
    html.dev-mode .dmj-sidebar {
      display: none;
    }
  }
</style>
